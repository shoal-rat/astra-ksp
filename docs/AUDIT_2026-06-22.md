# KSP Commander Audit — 2026-06-22

Run of the `ksp-commander-audit` subagent (`.claude/agents/ksp-commander-audit.md`). Verdict: the
agent flies as a "Space Commander" for the migrated phases (ascent, nodes, rendezvous/dock, and the
dedicated `tools/mj_land_vessel.py`), but the **landing/reentry migration is unfinished** — the live
crew-return and Mun-landing paths still hand-roll descent.

## Applied this pass (safe restructurings)
- **`docs/USING_KRPC_AND_MECHJEB.md` §7** reorganized into phase subsections (7.1 Ascent · 7.2
  Transfer & nodes · 7.3 SOI capture · 7.4 Rendezvous & docking · 7.5 Landing & reentry · 7.6 Frames,
  warp & control · 7.7 Discipline), each lesson in symptom → cause → fix form; duplicates merged, no
  fact deleted, the three newest lessons preserved last.
- **Deprecation banners** added to `tools/recover_crew.py`, `tools/return_and_recover.py`,
  `tools/test_chute.py`: superseded by `tools/mj_land_vessel.py`; keep only for the
  jettison-then-MechJeb-land case. No logic changed.
- **New ledger rule** (`src/ksp_lab/astra/ledger.py` SEED_RULES): a reentry-crash / chuteless /
  recovery-timeout marker now maps to "Delegate reentry to MechJeb's Landing Autopilot" so the agent's
  auto-diagnosis recommends `/mj-land` instead of re-guessing.

## Area 1 — hand-rolled descent on the LIVE path
1. ✅ **DONE** — `src/ksp_lab/flight_controller.py` `_recover_on_kerbin` (the live Orion reentry,
   reached by `tools/fly_orion.py` → `run_orion_return`) has been **migrated to `/mj-land`**: it now
   makes the capsule active, hands the whole descent to MechJeb's Landing Autopilot (which computes the
   deorbit + deceleration + parachute timing), warp-assists only the high coast, and monitors. The
   guessed `surface_altitude < 8_000` chute trigger and the `warp_to(periapsis)` hang are gone.
   53 tests pass. (PROPOSED items below remain.)
2. `src/ksp_lab/flight_controller.py` `_land_on_mun` (live HLS sortie landing) + the `guidance.py`
   terminal-throttle ladder — mostly derived hoverslam math, but hand-tuned terminal literals; defer
   to `/mj-land` or document the literals as tuned, not load-bearing.
3. `tools/recover_crew.py`, `tools/return_and_recover.py` (reentry half), `tools/resume_mun_descent.py`,
   `tools/test_chute.py` — un-migrated hand-rolled descent with magic constants (chute-arm 14 km,
   deorbit pe 24-32 km, 240/400 s timeouts). Keep only `return_and_recover.py`'s jettison step.

Correctly delegated (verified): `mj_land_vessel.py` (exemplary — call `/mj-land`, monitor only),
`mj_to_orbit.py` (`/mj-ascent` + documented pad kick), `mj_to_mun.py` (`/mj-execute-node` + documented
retrograde capture), `fly_mj_dock.py` (`/mj-rendezvous` → `/mj-dock`).

## Area 4 — bridge MechJeb/kRPC surface gaps (PROPOSED — each removes a hand-rolled/guessed step)
Exposed today: `/mj-ascent`, `/mj-execute-node`, `/mj-rendezvous`, `/mj-dock`, `/mj-land`,
`/mj-disable`, `/mj-status`. Recommended additions (rebuild bridge; verify member names vs installed
`MechJeb2.dll`):
1. **Maneuver planner** (`MechJebModuleManeuverPlanner` + OperationCircularize / Transfer /
   MoonReturn / Plane) → `POST /mj-plan` — MechJeb CREATES nodes instead of the kRPC grid search.
2. **Plane-match** (planner's OperationPlane) → `POST /mj-match-plane` — the open ~1140 m/s
   docking-roadmap case.
3. **Warp controller** (`MechJebModuleWarpController`) → `POST /mj-warp {to}` — replaces every
   hand-rolled `sc.warp_to(...)` lead-time guess and the documented `warp_to(periapsis)` hang.
4. **VesselState / SmartASS readouts** (`MechJebModuleVesselState`) → `GET /mj-vesselstate` — live
   stage Δv, total Δv, TWR, terminal velocity, **suicide-burn altitude**. Highest leverage: the agent
   READS MechJeb's computed numbers instead of guessing/recomputing. Directly serves "calculate, never
   guess."

## Other proposed (not applied)
- Deprecate the hand-rolled `flight_controller.run_dock_and_transfer` rendezvous/dock stack in favor
  of `/mj-dock` (it's ~700 lines and live, so a code pass, not a banner).

## Verification
- `pytest -q` → 53 passed. `py_compile` of the three deprecated tools → OK.
